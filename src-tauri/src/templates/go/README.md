# {{project_name}}

A Go web API built with Gin.

## Getting Started

```bash
go mod tidy
go run main.go
```

Server starts on `http://localhost:3000`.

## Endpoints

- `GET /health` — health check
- `GET /api/v1` — root endpoint
