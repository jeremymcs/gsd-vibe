# {{project_name}}

An Express REST API with TypeScript.

## Getting Started

```bash
npm install
npm run dev
```

## Endpoints

- `GET /health` — health check
- `GET /api/v1` — root API endpoint

## Scripts

- `npm run dev` — start with hot reload
- `npm run build` — compile TypeScript
- `npm run start` — run compiled output
