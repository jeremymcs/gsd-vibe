# {{project_name}}

A FastAPI application with Python.

## Getting Started

```bash
uv sync
uv run python run.py
```

Or with pip:

```bash
pip install -e .
python run.py
```

API docs at `http://localhost:8000/docs`.

## Endpoints

- `GET /health` — health check
- `GET /api/v1` — root endpoint
