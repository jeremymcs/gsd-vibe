[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2021"
description = "A command-line tool"

[[bin]]
name = "{{project_name}}"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[profile.release]
opt-level = "s"
strip = true
