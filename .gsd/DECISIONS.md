# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M004 | arch | Branch reconciliation strategy for M003 | Manual merge with main as base — cherry-pick M003 additions into main's codebase rather than git merge | The M003 branch diverged from a pre-M001/M002 ancestor (3cc25cb). A git merge produces 8 conflicts including an architectural conflict in project.tsx (Tabs vs nav-rail). Manual cherry-picking of M003's unique additions into main preserves main's nav-rail architecture and avoids the risk of accidentally reverting M002 code. The 8 conflict files are all integration points where both branches added code — manual merge ensures both sides' additions are preserved. | No | agent |
| D002 | M004/S04 | arch | Settings panels gap recovery approach | Rebuild from DECISIONS.md specifications (D017, D018, D019) and M002 slice summaries | The settings panels code (gsd2_get_settings, gsd2_save_preferences, gsd2_save_remote_token + SettingsPanel) exists on neither main nor the M003 branch despite being documented as validated. The decisions register contains detailed implementation specifications: D017 (serde_yaml::Value intermediate representation for YAML parsing), D018 (merge_preferences returns values + scope annotation map), D019 (keyring token storage with bool-only IPC exposure). These are specific enough to rebuild from without re-discussing architecture. | No | agent |
| D003 | M004/S01 | implementation | How to reconstruct M003 code when the M003-k8v2px git branch is absent | Use .gsd/milestones/M003-k8v2px/slices/*/tasks/*-SUMMARY.md as the authoritative implementation source | The M003-k8v2px git branch does not exist in any local or remote repository. The GSD task summary files contain exact field names, function signatures, struct definitions, and implementation details sufficient to reconstruct the code without the branch. This approach is preferable to asking for branch restoration because the summaries are already available and complete. | No | agent |
| D004 | M004/S01/T02 | typescript | How to handle TypeScript types in queries.ts body under noUnusedLocals strict mode | Remove inline import type statements and use api.TypeName namespaced references where explicit type annotation is required | TypeScript's noUnusedLocals strict mode (tsconfig strict: true) flags inline import type statements in the file body as TS6133/TS6192 errors when the imported types are not directly referenced in code — the hooks use api.* for type inference and return type annotation. Using api.TypeName as a namespaced reference satisfies the type system without triggering the unused-import diagnostic. | Yes | agent |
| D005 | M004/S02 | arch | How to wire overlay components (UpdateBanner, OnboardingWizard) vs nav-rail view components (SessionBrowser, MaintenanceTab) | Overlays render directly in ProjectPage guarded by isGsd2; view components route through ViewRenderer switch cases | UpdateBanner and OnboardingWizard are cross-cutting concerns that should appear regardless of which nav-rail view is active. They are not views themselves — they are contextual overlays. Routing them through ViewRenderer would hide them whenever a different view is selected. SessionBrowser and MaintenanceTab are full-panel views that replace the main content area, so they belong in ViewRenderer. | No | agent |
| D006 | M004/S02 | typescript | Where to place ExportPanel in the UI hierarchy | Inside gsd2-visualizer-tab.tsx at the bottom of the timeline section, not as a standalone nav-rail view | ExportPanel is a utility action that contextually belongs with the visualizer (metrics/timeline/export together). Making it a separate nav view would orphan it from the data context. Embedding it in the visualizer tab keeps the export action adjacent to the data being exported. | No | agent |
| D007 | M004/S02 | typescript | TanStack Query mutation stub shape for TypeScript cast in tests | Full 14-field stub required: mutate, mutateAsync, isPending, isSuccess, isIdle, status, data, error, reset, variables, context, failureCount, failureReason, submittedAt | TypeScript's ReturnType<typeof useGsd2ExecuteCleanup> includes all UseMutationResult fields. A partial stub causes TS2352 'conversion of type X to type Y may be a mistake'. The full 14-field stub satisfies the cast without using 'as unknown as' double cast. | No | agent |
| D008 | M004/S02 | typescript | useGsd2ValidateProviderKey hook signature — does it accept projectId? | No — takes only { providerId, apiKey }. StepAuthenticate accepts projectId prop for future extensibility but does not forward it | The Rust backend command gsd2_validate_provider_key only takes two parameters. Passing projectId would cause a TypeScript error at the call site. The prop is accepted at the component boundary for interface stability (callers can pass projectId) but is not forwarded to the mutation. | No | agent |
| D009 | M004/S03/T01 | typescript | Placement of gsd2Models query key in query-keys.ts | New `// GSD-2 M004 keys` section after M003 section | Maintains chronological milestone-based organization pattern established in M003. Mixing M004 keys into M003 section would blur provenance. | Yes | agent |
| D010 | M004/S03/T01 | implementation | gsd2ListModels hook — should it have an enabled guard like project-scoped hooks? | No enabled guard — useGsd2ListModels is always enabled | gsd2_list_models takes only `search: Option<String>` with no projectId dependency. It is a global model registry lookup, not project-scoped. Adding an enabled guard would cause it to never fire. | No | agent |
| D011 | M004/S03/T02 | implementation | How to recover session ID after gsd2HeadlessStartWithModel | Call gsd2HeadlessGetSession after mutation success — same mount-effect recovery pattern already in the component | gsd2_headless_start_with_model returns void (unlike gsd2_headless_start which returns a session ID string). The component already has a session recovery pattern via gsd2HeadlessGetSession on mount; reusing it keeps the code consistent and avoids divergent session-ID resolution paths. | No | agent |
| D012 | M004/S03/T02 | typescript | TypeScript narrowing error inside {isIdle && ...} conditional block | Hoist `const isRunning = status === 'running'` before the isIdle check | Inside `{isIdle && ...}`, TypeScript narrows `status` to exclude 'running', making `status === 'running'` a type error. Hoisting the boolean check outside the conditional block captures the pre-narrowing value and satisfies the type checker without suppression comments. | No | agent |
| D013 | M004/S03/T03 | testing | gsd2ResolveCapture test argument count | Use all 5 required args (projectId, captureId, classification, resolution, rationale) not the simplified 2-arg plan description | The actual tauri.ts signature requires 5 args. Writing the test with fewer args would produce a TypeScript error at compile time and a false test at runtime. Always check the actual wrapper signature before writing test assertions. | No | agent |
| D014 | M004/S04/T01 | implementation | Rust test file isolation strategy for settings unit tests | Use tempfile crate (dev-dependency) for scratch files instead of std::env::temp_dir() | tempfile::NamedTempFile provides RAII cleanup — temp files are automatically deleted when the value is dropped, even on test failure. std::env::temp_dir() requires manual cleanup. tempfile is already widely used in the Rust ecosystem and the overhead of adding a dev-dep is negligible. | Yes | agent |
| D015 | M004/S04/T01 | implementation | gsd2_get_settings uses project_path not project_id as identifier | Accept project_path string, not a DB project_id integer | All other path-based commands in gsd2.rs (gsd2_run, gsd2_execute_auto, etc.) take project_path directly to avoid a DB lookup per call. Settings are stored in filesystem files (preferences.md) relative to project_path, so project_path is the natural key — no JOIN needed. | No | agent |
| D016 | M004/S04/T01 | implementation | SaveResult struct is distinct from existing WriteResult — new type with ok/error fields | Define a new SaveResult struct with `ok: bool` + `error: Option<String>` rather than reusing WriteResult | The existing WriteResult has success/auth_path/preferences_path fields designed for onboarding flow output. Settings save mutations need a simpler boolean outcome with an optional error message for the UI feedback surface. Adding a new struct avoids coupling the settings save API shape to the onboarding API shape. | No | agent |
| D017 | M004/S04/T02 | typescript | SettingsPanel collapsible sections implemented with useState not shadcn/Collapsible | Plain useState + conditional render for section expand/collapse | The shadcn Collapsible primitive is not installed in the project. Adding it would require a new dependency install. The same UX (click header to expand/collapse) is achievable with useState + a show/hide boolean and a chevron icon. No new dependency is introduced. | Yes | agent |
| D018 | M004/S04/T02 | security | Token input state cleared immediately after successful save in RemoteQuestionsSection | setToken('') after mutation.isSuccess — token not retained in React state | The remote questions token is a credential. Retaining it in React state beyond the minimum required window (user typed it → clicked save → save confirmed) violates the plan's redaction constraint. Clearing immediately on success matches the principle of least retention. | No | agent |
| D019 | M005 | convention | Animation approach for polish pass | CSS-only transitions — no animation library | Per user direction: "tasteful CSS transitions" not framer-motion. The app is a Tauri desktop tool, not a marketing site. CSS transitions (crossfades, hover lifts, staggers via CSS animation-delay) achieve the desired feel without adding a dependency. prefers-reduced-motion support is already wired in globals.css. | No | human |
| D020 | M005 | arch | Light theme strategy | Add `:root` CSS variables block to globals.css alongside existing `.dark` block | The ThemeProvider already applies `.light` or `.dark` class to `document.documentElement`. Adding a `:root` block (which applies when `.light` is present) mirrors the existing `.dark` pattern exactly. No component changes needed — all components already use HSL CSS variables. | No | agent |
| D021 | M005 | arch | Shared state pattern primitive location | New `src/components/shared/loading-states.tsx` with ViewSkeleton, ViewError, ViewEmpty | Centralizing the three-state pattern in a shared module ensures every view uses the same markup, icon sizes, and copy conventions. Individual views import the primitives and supply context-specific messages — they do not own the layout of the state container. | No | agent |
| D022 | M005 | library | highlight.js bundle reduction approach | Switch to `highlight.js/lib/core` + explicit language imports; enumerate languages from actual project file extensions | Full highlight.js (192 languages) is 1.2MB. `highlight.js/lib/core` with 15 common languages (ts, js, rust, python, bash, json, yaml, css, html, markdown, go, cpp, java, sql, toml) achieves coverage of all realistic knowledge base content at ~15% of the bundle size. hljs.highlightAuto is replaced with hljs.highlight + language detection from file extension. | Yes | agent |
| D023 | M005/S01 | arch | Light theme CSS selector: `.light {}` vs `:root` | Use `.light {}` selector inside `@layer base`, NOT `:root` — refines D020 | D020 stated `:root` but `:root` applies regardless of theme class and would override dark-mode variables. The ThemeProvider adds `.light` class to `<html>`, so `.light {}` is the correct selector — it only activates when light theme is selected, mirroring the existing `.dark {}` pattern exactly. This is a selector correction, not a strategy change. | No | agent |
| D024 | M005/S02 | react-best-practices | Conditional rendering pattern for optional ReactNode props | Use ternary `{condition ? <Element /> : null}` NOT `{condition && <Element />}` | The react-best-practices skill `rendering-conditional-render` rule flags logical AND as a bug vector: if the condition evaluates to 0 or false-y string, React renders that value instead of nothing. Ternary with explicit null is type-safe and always renders null when false. Applied to ViewError icon and ViewEmpty description props. | No | agent |
| D025 | M005/S02 | implementation | Error card structure in roadmap-progress-card.tsx | Wrap ViewError inside the same Card/CardHeader/CardContent shell, not bare ViewError | The roadmap-progress-card shows "Roadmap Progress" as the title even when data is loading or failed. Wrapping ViewError in the same Card shell preserves the title visibility on error — consistent with the existing empty-state behavior that also keeps the title. A bare ViewError would hide the title, breaking the card structure. | No | agent |
| D026 | M005/S03 | animation | prefers-reduced-motion block must include opacity:1 alongside animation:none for animations that start from opacity:0 | Always add `opacity: 1` alongside `animation: none !important` in the prefers-reduced-motion block for animate-shimmer, animate-stagger-in, and any future class that starts from opacity:0 | @keyframes stagger-in starts from opacity:0 + translateY(8px). Without opacity:1, reduced-motion users see permanently invisible content. The existing reduced-motion block only had animation:none — the opacity:1 companion was added in S03 and must be maintained for all future opacity-animated classes. | No | agent |
| D027 | M005/S04 | accessibility | Whether to add role="navigation" to `<aside>` in main-layout.tsx | Leave `<aside>` without role="navigation" — preserve implicit complementary landmark | Adding role="navigation" to `<aside>` would conflict with its implicit ARIA role of complementary and break the pre-existing `getByRole("complementary")` test. The `<nav>` element inside the aside correctly carries role="navigation" with a named aria-label. Nesting a navigation inside a complementary landmark is valid ARIA — no outer role override needed. | No | agent |
| D028 | M005/S05 | implementation | highlight.js language selection for EXTENSION_TO_LANGUAGE map — final set and correction to D022 | 40 languages from the actual EXTENSION_TO_LANGUAGE map in file-browser.tsx; tf→'ini' (not 'hcl'); sol entry removed (not mapped to another language) | D022 estimated "15 common languages" but the actual EXTENSION_TO_LANGUAGE map in file-browser.tsx contained 40 distinct language names. hcl and solidity do not exist as highlight.js language modules — attempting to import them would cause build errors. tf is remapped to 'ini' as the closest structural approximation (both use key=value and block syntax). sol is removed entirely: .sol files fall to 'plaintext' default, which is more honest than an incorrect approximation. The 40-language count includes plaintext which is the registered fallback. | Yes | agent |
| D029 | M005/S05 | build | Whether to keep or remove the 'highlight.js' entry in vite.config.ts manualChunks after switching file-browser.tsx to selective imports | Remove 'highlight.js' from vendor-markdown manualChunks after switching to selective imports | With T01's selective imports in place, the bare 'highlight.js' entry in manualChunks would cause Rollup to bundle the full module redundantly rather than tree-shaking to only the core + 40 languages. The manualChunks entry forces Rollup to include the package as a chunk input — it doesn't know to tree-shake based on actual imports. Removing it lets Rollup include only what's actually imported from highlight.js/lib/core and highlight.js/lib/languages/*. | No | agent |
| D030 | M005/S06/T01 | convention | How to silence dead-code Rust warnings for test-only items vs truly unused items | Item-level `#[allow(dead_code)]` for test-only items; outright deletion for items with zero callers anywhere in the codebase | File-level `#![allow(dead_code)]` would silence all warnings in a file, masking future regressions. Item-level suppression is scoped and intentional. Items confirmed to have zero callers (Decision struct, list_sessions) are deleted rather than suppressed — deletion is the strongest signal that the code is not needed and eliminates noise permanently. | No | agent |
